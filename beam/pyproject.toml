[project]
name = "beam"
version = "0.1.0"
description = "Apache Beam Python project for Flink"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "apache-beam>=2.71.0",
]

[project.scripts]
run-job = "beam_job:run"

[tool.uv]
prerelease = "allow"
